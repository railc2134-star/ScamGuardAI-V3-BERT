# Phishing Scam Detector

This project uses the BERT model to detect phishing scams in messages.
It trains a model on a dataset of labeled messages and then evaluates its performance on a test set.
The project utilizes the Transformers library and PyTorch for building and training the model.
To run the project, make sure to install the required libraries and datasets, then execute the transformerSCAM2.py script.